---
type: pull_request
state: closed (merged)
branch: feature/52-post-release-dev-protection → main
created: 2026-03-02T11:16:34Z
updated: 2026-03-12T14:07:44Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/sync-issues-action/pull/59
comments: 7
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-12T14:07:43Z
synced: 2026-07-16T12:37:55.210Z
---

# [PR 59](https://github.com/vig-os/sync-issues-action/pull/59) fix(ci): split sync workflow auth between commit and release apps

## Description

This PR finalizes the branch-protection-safe sync flow by tightening auth boundaries and workflow behavior across both sync workflows.

It separates GitHub App responsibilities (commit/ref operations vs PR/label operations), updates issue-sync auth to the commit app, and ensures manual sync output directory input is respected.

## Type of Change

- [ ] `feat` -- New feature
- [x] `fix` -- Bug fix
- [ ] `docs` -- Documentation only
- [ ] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [ ] `test` -- Adding or updating tests
- [x] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- `.github/workflows/sync-main-to-dev.yml`
  - Split auth into `COMMIT_APP_*` (checkout/git refs) and `RELEASE_APP_*` (PR/labels)
  - Keep stale sync-branch git-ref cleanup on commit-scoped token
  - Generate release token only when re-check confirms sync work is still needed
  - Keep conflict-path behavior and duplicate-PR guard intact
- `.github/workflows/sync-issues.yml`
  - Switch app auth secrets to `COMMIT_APP_ID` / `COMMIT_APP_PRIVATE_KEY`
  - Pass `workflow_dispatch` `output-dir` input through to the action with `docs` fallback
- `CHANGELOG.md`
  - Updated `Unreleased` notes for issue #52 to capture token split and sync-issues workflow updates

## Changelog Entry

### Changed

- **Post-release replaced by PR-based main-to-dev sync** ([#52](https://github.com/vig-os/sync-issues-action/issues/52))
  - Remove `post-release.yml` workflow; add `sync-main-to-dev.yml` that opens a PR to sync `main` into `dev`, satisfying branch protection on both branches
  - Harden sync checks by failing clearly when `origin/main` or `origin/dev` is missing instead of silently treating branches as up to date
  - Fix `workflow_dispatch` hyphenated input access and conflict path robustness (`issues: write`, safer conflict label handling, clearer manual resolution commands)
  - Reduce duplicate/no-op sync PR risk by re-checking ahead/behind state inside the `sync` job and tightening existing-sync-PR detection (search + explicit list limit)
  - Split auth into explicit app tokens by responsibility: `COMMIT_APP_*` for checkout/ref operations and `RELEASE_APP_*` for PR/label operations requiring broader scopes
  - Generate `RELEASE_APP_*` only after re-check confirms sync work remains, reducing unnecessary broader-scope token issuance
- **Sync workflow uses commit-scoped app secrets and manual output target**
  - Update `sync-issues.yml` to use `COMMIT_APP_ID`/`COMMIT_APP_PRIVATE_KEY` for both checkout token generation and action app auth inputs
  - Respect `workflow_dispatch` `output-dir` input in the action call, with `'docs'` as the default fallback

## Testing

- [x] Tests pass locally (`npm test`) — 95/95 passed
- [x] CHANGELOG validation passed (`prepare_changelog.py validate`)
- [x] Manual testing performed (conflict path and duplicate-PR guard)

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [x] I have updated the documentation accordingly
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [x] My changes generate no new warnings or errors
- [x] New and existing unit tests pass locally with my changes

## Additional Notes

Post-merge validation on `main` should still confirm no-op early exit and clean no-conflict PR creation paths.

Refs: #52



---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/sync-issues-action/pull/59#issuecomment-4024789488) by [@c-vigo](https://github.com/c-vigo)

_Posted on March 9, 2026 at 03:52 PM_

@gerchowl reminder

---
---

## Review Threads (3)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 2, 2026 at 11:20 AM_

_File: [`.github/workflows/sync-main-to-dev.yml (line 18 RIGHT)`](https://github.com/vig-os/sync-issues-action/pull/59#discussion_r2871877495)_

```diff
@@ -12,8 +12,9 @@
 #         - open PR (auto-mergeable, or labelled "merge-conflict" with
 #           resolution instructions when conflicts exist)
 #
-# Auth: GitHub App token (COMMIT_APP_ID / COMMIT_APP_PRIVATE_KEY), a dedicated
-# secret set for git/API operations requiring app identity and signing.
+# Auth: Two GitHub App tokens:
+#   - COMMIT_APP_* for git/ref operations (least-privilege commit identity)
+#   - RELEASE_APP_* for PR/label operations that require pull-request scopes
 #
```

The header comment says RELEASE_APP_* is only for PR/label operations, but later the workflow uses the release token for git ref operations (stale branch listing/deletion). Either update the header to reflect actual usage or (preferred for least privilege) move those git/ref calls to the commit app token so the documentation matches behavior.
```suggestion
#   - COMMIT_APP_* for most git/ref operations (least-privilege commit identity)
#   - RELEASE_APP_* for PR/label operations that require pull-request scopes
#       and for stale sync branch listing/deletion (some git/ref operations)
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 2, 2026 at 12:36 PM — [link](https://github.com/vig-os/sync-issues-action/pull/59#discussion_r2872210254)

  Implemented in e444826: stale-branch ref list/delete now run with `COMMIT_APP_*`, and only the PR existence check uses `RELEASE_APP_*` inline. This keeps docs and behavior aligned with least-privilege token responsibilities.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 2, 2026 at 11:20 AM_

_File: [`.github/workflows/sync-main-to-dev.yml (line 174 RIGHT)`](https://github.com/vig-os/sync-issues-action/pull/59#discussion_r2871877525)_

```diff
@@ -161,7 +169,7 @@ jobs:
       - name: Clean up stale sync branches
         if: steps.recheck.outputs.up_to_date != 'true'
         env:
-          GH_TOKEN: ${{ steps.app-token.outputs.token }}
+          GH_TOKEN: ${{ steps.release-app-token.outputs.token }}
         run: |
```

This step uses the RELEASE_APP token for git reference operations (`git/matching-refs` and deleting refs). If the intent is least privilege (commit app for git/ref, release app for PR/label), consider using the commit app token for the gh api ref-list/delete calls and only the release app token for the `gh pr list` checks within the loop (you can override GH_TOKEN per command).

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 2, 2026 at 12:36 PM — [link](https://github.com/vig-os/sync-issues-action/pull/59#discussion_r2872210339)

  Updated in e444826: `Clean up stale sync branches` now defaults to `COMMIT_APP_*` for git ref operations, while `gh pr list` is explicitly scoped to `RELEASE_APP_*` per-command. This applies the split suggested in this thread.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 2, 2026 at 12:53 PM_

_File: [`.github/workflows/sync-main-to-dev.yml`](https://github.com/vig-os/sync-issues-action/pull/59#discussion_r2872283756)_

```diff
@@ -94,19 +95,26 @@ jobs:
       issues: write
 
     steps:
-      - name: Generate GitHub App Token
-        id: app-token
+      - name: Generate Commit App Token
+        id: commit-app-token
         uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf  # v2
         with:
           app-id: ${{ secrets.COMMIT_APP_ID }}
           private-key: ${{ secrets.COMMIT_APP_PRIVATE_KEY }}
 
+      - name: Generate Release App Token
+        id: release-app-token
+        uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf  # v2
+        with:
+          app-id: ${{ secrets.RELEASE_APP_ID }}
+          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
+
```

The Release App token is generated unconditionally, even though later steps may exit early when `steps.recheck.outputs.up_to_date == 'true'`. To minimize issuance/exposure of the broader-scope token, generate the release token only after the recheck confirms work is needed (e.g., move this step below `Re-check if dev is still behind main` and gate it with the same `if`).
```suggestion

```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 2, 2026 at 01:41 PM — [link](https://github.com/vig-os/sync-issues-action/pull/59#discussion_r2872525083)

  Implemented in 4a46034: moved `Generate Release App Token` to run after `Re-check if dev is still behind main` and gated it with `if: steps.recheck.outputs.up_to_date != true`. This avoids issuing the broader-scope token when recheck determines there is no remaining sync work.


---
---

## Commits

### Commit 1: [acdafc6](https://github.com/vig-os/sync-issues-action/commit/acdafc6c5caeb2298bc383733eee3fb59294d247) by [c-vigo](https://github.com/c-vigo) on March 2, 2026 at 11:11 AM
fix(ci): split sync workflow auth between commit and release apps, 29 files modified (.github/workflows/sync-main-to-dev.yml, CHANGELOG.md)

### Commit 2: [659122d](https://github.com/vig-os/sync-issues-action/commit/659122d734306b01c8a2651f963fea5b616aedd5) by [c-vigo](https://github.com/c-vigo) on March 2, 2026 at 11:14 AM
Merge remote-tracking branch 'origin/main' into feature/52-post-release-dev-protection

### Commit 3: [e444826](https://github.com/vig-os/sync-issues-action/commit/e444826e8b7a4ed1324ed054b30bd4cdb3295729) by [c-vigo](https://github.com/c-vigo) on March 2, 2026 at 12:29 PM
fix(ci): use commit token for stale sync git refs, 4 files modified (.github/workflows/sync-main-to-dev.yml)

### Commit 4: [4a46034](https://github.com/vig-os/sync-issues-action/commit/4a46034d7e503f9d5303394cc1fee68bcfdf4d6b) by [c-vigo](https://github.com/c-vigo) on March 2, 2026 at 01:41 PM
fix(ci): defer release token creation until recheck, 16 files modified (.github/workflows/sync-main-to-dev.yml, CHANGELOG.md)

### Commit 5: [de9568e](https://github.com/vig-os/sync-issues-action/commit/de9568eb588396a7635ca74acd6c61245f8d2b91) by [c-vigo](https://github.com/c-vigo) on March 5, 2026 at 02:57 PM
chore: update GitHub App token secrets for issue synchronization, 13 files modified (.github/workflows/sync-issues.yml, CHANGELOG.md)

### Commit 6: [c617142](https://github.com/vig-os/sync-issues-action/commit/c6171427898c64c384d26c3f9b4b22fa5c87a742) by [c-vigo](https://github.com/c-vigo) on March 11, 2026 at 03:09 PM
fix(ci): remove invalid output-dir input reference in sync workflow, 2 files modified (.github/workflows/sync-issues.yml)
